# Progression & Unlocks

- No classes; players unlock starting presets (starting stats and starting items) between runs.
- Relics and presets can be persisted between runs (local storage; optional server sync later).
- Presets and unlocks must use multiples-of-5 values.
